import { Box, Typography } from '@mui/material';

export default function BooksFilters() {
  return (
    <Box sx={{ backgroundColor: 'lightpink', border: '2px dashed red' }}>
      <Typography variant="h5">Books Filters</Typography>
    </Box>
  );
}
